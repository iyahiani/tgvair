package com.avancial.test;

import org.junit.Test;

public class TestPropertiyConf {

   @Test
   public void testPropConf() {
/*
     // ServletContext context = Ser                       ;
      String log4jConfigFile = context.getInitParameter("log4j-config-location")   ;
      String path = context.getRealPath("") + File.separator + log4jConfigFile ;
     // String path = "log4j.properties";
      Properties prop = new Properties() ; 
      InputStream inputStream = getClass().getClassLoader().getResourceAsStream(path) ;
      if(null != inputStream ) {
         try {
            prop.load(inputStream);  
            prop.setProperty("log4j.appender.sql.URL", "sdsd"); //jdbc:mysql://caliban/tgv_air
            
            
         } catch (IOException e) {
         
            System.out.println(e.getMessage());
         } 
         
      }*/
   }

}
